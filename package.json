{
  "name": "seminario-api-rest-orders",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API REST de pedidos con TypeScript, OpenAPI e idempotencia",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "tsx scripts/demo.ts"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "fastify": "^5.6.0",
    "yaml": "^2.8.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
